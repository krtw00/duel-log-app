# フィードバック・連絡先機能 設計書

## 1. 概要

### 1.1 目的
ユーザーがアプリ内から簡単にバグ報告や機能要望を送信でき、また開発者への連絡手段を提供する。

### 1.2 機能一覧
| 機能 | 説明 |
|------|------|
| バグ報告フォーム | GitHub Issuesへ自動的にIssueを作成 |
| 機能要望フォーム | GitHub Issuesへ自動的にIssueを作成 |
| 連絡先表示 | メールアドレス、SNSリンクを表示 |

---

## 2. UI設計

### 2.1 配置場所

AppBarに「ヘルプメニュー」を追加（「?」アイコン）

```
[DUEL LOG]  [ダッシュボード] [デッキ管理] [統計]  [🌙] [🌐] [❓] [ユーザー▼]
                                                         ↑
                                                    ヘルプメニュー
```

### 2.2 ヘルプメニュー構成

```
┌─────────────────────────┐
│ ❓ ヘルプ               │
├─────────────────────────┤
│ 🐛 バグを報告           │
│ 💡 機能をリクエスト     │
├─────────────────────────┤
│ 📧 お問い合わせ         │
│ 🐦 Twitter/X            │
├─────────────────────────┤
│ ℹ️  バージョン: 1.10.1  │
└─────────────────────────┘
```

### 2.3 バグ報告ダイアログ

```
┌─────────────────────────────────────────┐
│ 🐛 バグを報告                      [×] │
├─────────────────────────────────────────┤
│                                         │
│ タイトル *                              │
│ ┌─────────────────────────────────────┐ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 説明 *                                  │
│ ┌─────────────────────────────────────┐ │
│ │ 問題の詳細を教えてください          │ │
│ │ - 何をしようとしましたか？          │ │
│ │ - 何が起きましたか？                │ │
│ │ - 期待した動作は？                  │ │
│ └─────────────────────────────────────┘ │
│                                         │
│              [キャンセル] [送信]        │
└─────────────────────────────────────────┘
```

### 2.4 機能リクエストダイアログ

```
┌─────────────────────────────────────────┐
│ 💡 機能をリクエスト                [×] │
├─────────────────────────────────────────┤
│                                         │
│ タイトル *                              │
│ ┌─────────────────────────────────────┐ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 説明 *                                  │
│ ┌─────────────────────────────────────┐ │
│ │ どんな機能が欲しいですか？          │ │
│ │ - どんな問題を解決しますか？        │ │
│ │ - どのように使いたいですか？        │ │
│ └─────────────────────────────────────┘ │
│                                         │
│              [キャンセル] [送信]        │
└─────────────────────────────────────────┘
```

---

## 3. 技術設計

### 3.1 GitHub Issues連携

#### 3.1.1 方式選定

**採用方式**: GitHub REST API（Personal Access Token使用）

フロントエンド → バックエンドAPI → GitHub API

```
[ユーザー] → [フロントエンド] → [バックエンドAPI] → [GitHub API]
                                    ↓
                              Issue作成
```

#### 3.1.2 なぜバックエンド経由か

- Personal Access Tokenをフロントエンドに露出させない
- レート制限の管理
- スパム対策（認証ユーザーのみ送信可能）

#### 3.1.3 バックエンドAPI設計

**エンドポイント**:
```
GET  /api/feedback/status        # フィードバック機能の状態を取得
POST /api/feedback/bug           # バグ報告
POST /api/feedback/enhancement   # 機能要望
POST /api/feedback/contact       # お問い合わせ
```

**リクエストボディ（バグ報告）**:
```json
{
  "title": "string",
  "description": "string",
  "steps": "string | null",      // 再現手順
  "expected": "string | null",   // 期待する動作
  "actual": "string | null"      // 実際の動作
}
```

**リクエストボディ（機能要望）**:
```json
{
  "title": "string",
  "description": "string",
  "use_case": "string | null"    // ユースケース
}
```

**リクエストボディ（お問い合わせ）**:
```json
{
  "subject": "string",
  "message": "string"
}
```

**レスポンス**:
```json
{
  "success": true,
  "message": "バグ報告を送信しました。ご報告ありがとうございます。",
  "issue_url": "https://github.com/owner/repo/issues/123",
  "issue_number": 123
}
```

**ステータスレスポンス（GET /status）**:
```json
{
  "github_enabled": true,
  "x_handle": "@duellog_app",
  "x_url": "https://x.com/duellog_app",
  "github_repo_url": "https://github.com/owner/repo"
}
```

#### 3.1.4 GitHub Issue テンプレート

> **プライバシー保護**: GitHub Issuesは公開されるため、ユーザーの個人情報（メールアドレス、ユーザー名、内部ID）は一切含めません。管理者が報告者を追跡する必要がある場合は、サーバーログを参照します。

**バグ報告のIssue内容**:
```markdown
## 説明
{ユーザーが入力した説明}

## 再現手順
{ユーザーが入力した再現手順}

## 期待する動作
{ユーザーが入力した期待する動作}

## 実際の動作
{ユーザーが入力した実際の動作}

---
**Environment:** `{User-Agent}`
```

**機能リクエストのIssue内容**:
```markdown
## 説明
{ユーザーが入力した説明}

## ユースケース
{ユーザーが入力したユースケース}

---
**Environment:** `{User-Agent}`
```

**お問い合わせのIssue内容**:
```markdown
## メッセージ
{ユーザーが入力したメッセージ}

---
**Environment:** `{User-Agent}`
```

#### 3.1.5 GitHub Issue ラベル

| フィードバックタイプ | 自動付与ラベル |
|---------------------|----------------|
| バグ報告 | `bug`, `user-report` |
| 機能リクエスト | `enhancement`, `user-report` |
| お問い合わせ | `question`, `user-report` |

### 3.2 バックエンド実装

#### 3.2.1 必要な環境変数

```bash
# .env
GITHUB_TOKEN=ghp_xxxxxxxxxxxx        # Personal Access Token
GITHUB_REPO_OWNER=your-username      # リポジトリオーナー
GITHUB_REPO_NAME=duel-log-app        # リポジトリ名
FEEDBACK_ENABLED=true                # フィードバック機能の有効/無効
```

#### 3.2.2 ファイル構成

```
backend/
├── app/
│   ├── api/
│   │   └── routers/
│   │       └── feedback.py          # フィードバックルーター
│   └── services/
│       └── github_service.py        # GitHubAPI連携サービス
```

#### 3.2.3 サーバーログによる追跡

報告者の追跡が必要な場合、サーバーログを参照します：

```python
# feedback.py での実装
if result:
    # 管理者追跡用にサーバーログに記録（GitHub Issueには個人情報を含めない）
    logger.info(
        f"Bug report submitted: user_id={current_user.id}, issue=#{result['number']}"
    )
```

**ログ検索例:**
```bash
# 特定のIssue番号から報告者を特定
grep "issue=#123" /var/log/app/feedback.log
# => Bug report submitted: user_id=456, issue=#123

# 特定ユーザーのフィードバック履歴を確認
grep "user_id=456" /var/log/app/feedback.log
```

#### 3.2.4 サービス実装概要

```python
# backend/app/services/github_service.py
class GitHubService:
    async def create_issue(
        self,
        title: str,
        body: str,
        issue_type: IssueType,
        user_agent: str | None = None,  # ブラウザ/OS情報のみ
    ) -> dict | None:
        """
        プライバシー保護のため、ユーザーの個人情報（メールアドレス、
        ユーザー名、内部ID）はIssueに含めません。
        """
        full_body = body
        if user_agent:
            full_body += f"\n\n---\n**Environment:** `{user_agent}`"

        # GitHub API でIssue作成
        ...
```

### 3.3 フロントエンド実装

#### 3.3.1 ファイル構成

```
frontend/
├── src/
│   ├── components/
│   │   └── common/
│   │       ├── HelpMenu.vue              # 新規: ヘルプメニュー
│   │       ├── BugReportDialog.vue       # 新規: バグ報告ダイアログ
│   │       └── FeatureRequestDialog.vue  # 新規: 機能リクエストダイアログ
│   ├── composables/
│   │   └── useFeedback.ts                # 新規: フィードバック送信ロジック
│   └── services/
│       └── api.ts                        # 既存: APIエンドポイント追加
```

#### 3.3.2 環境情報の収集

```typescript
// src/composables/useFeedback.ts
export function useEnvironmentInfo() {
  return {
    browser: getBrowserInfo(),
    os: getOSInfo(),
    screen_size: `${window.innerWidth}x${window.innerHeight}`,
    app_version: import.meta.env.VITE_APP_VERSION || 'unknown'
  }
}

function getBrowserInfo(): string {
  const ua = navigator.userAgent
  // ブラウザ判定ロジック
  if (ua.includes('Chrome')) return `Chrome ${/* version */}`
  if (ua.includes('Firefox')) return `Firefox ${/* version */}`
  if (ua.includes('Safari')) return `Safari ${/* version */}`
  return 'Unknown'
}

function getOSInfo(): string {
  const platform = navigator.platform
  const ua = navigator.userAgent
  // OS判定ロジック
  if (ua.includes('Windows')) return 'Windows'
  if (ua.includes('Mac')) return 'macOS'
  if (ua.includes('Linux')) return 'Linux'
  if (ua.includes('Android')) return 'Android'
  if (ua.includes('iOS')) return 'iOS'
  return platform
}
```

---

## 4. 連絡先情報

### 4.1 表示する連絡先

| 種類 | 値 | 用途 |
|------|-----|------|
| メール | support@example.com | サポート・お問い合わせ |
| Twitter/X | @duellog_app | 更新情報・軽い質問 |

※実際の値は環境変数または設定ファイルで管理

### 4.2 環境変数

```bash
# .env
SUPPORT_EMAIL=support@example.com
TWITTER_URL=https://twitter.com/duellog_app
# 将来拡張用
DISCORD_INVITE_URL=
```

### 4.3 フロントエンド設定

```typescript
// src/config/contact.ts
export const CONTACT_INFO = {
  email: import.meta.env.VITE_SUPPORT_EMAIL || 'support@example.com',
  twitter: import.meta.env.VITE_TWITTER_URL || 'https://twitter.com/duellog_app',
  // discord: import.meta.env.VITE_DISCORD_URL || null,
}
```

---

## 5. セキュリティ考慮事項

### 5.1 スパム対策

| 対策 | 説明 |
|------|------|
| 認証必須 | ログインユーザーのみフィードバック送信可能 |
| レート制限 | 1ユーザーあたり1時間に5件まで |
| 入力検証 | タイトル・説明の最大文字数制限 |

### 5.2 入力制限

| フィールド | 最小 | 最大 |
|-----------|------|------|
| タイトル | 5文字 | 100文字 |
| 説明 | 20文字 | 2000文字 |

### 5.3 GitHub Token権限

必要な最小権限:
- `repo` → `public_repo`（パブリックリポジトリの場合）
- Issue作成のみに使用

---

## 6. 実装フェーズ

### Phase 1: 基本機能

1. [ ] バックエンドAPIの実装
   - [ ] feedback.pyルーター作成
   - [ ] FeedbackService実装
   - [ ] スキーマ定義
2. [ ] フロントエンドUI実装
   - [ ] HelpMenu.vue作成
   - [ ] BugReportDialog.vue作成
   - [ ] FeatureRequestDialog.vue作成
3. [ ] AppBar.vueにヘルプメニュー追加
4. [ ] 多言語対応（翻訳キー追加）

### Phase 2: 改善

1. [ ] レート制限の実装
2. [ ] 送信履歴の表示（任意）
3. [ ] スクリーンショット添付機能（任意）

---

## 7. 翻訳キー

`internationalization.md` に以下の翻訳キーを追加:

```json
{
  "help": {
    "title": "ヘルプ",
    "bugReport": "バグを報告",
    "featureRequest": "機能をリクエスト",
    "contact": "お問い合わせ",
    "twitter": "Twitter/X",
    "version": "バージョン"
  },
  "feedback": {
    "bugReport": {
      "title": "バグを報告",
      "titleLabel": "タイトル",
      "titlePlaceholder": "問題を簡潔に説明",
      "descriptionLabel": "説明",
      "descriptionPlaceholder": "問題の詳細を教えてください\n- 何をしようとしましたか？\n- 何が起きましたか？\n- 期待した動作は？",
      "includeEnvironment": "環境情報を含める",
      "includeEnvironmentHint": "ブラウザ、OS、画面サイズ",
      "anonymous": "匿名で送信",
      "anonymousHint": "ユーザー名・メールを含めない",
      "submit": "送信",
      "success": "バグ報告を送信しました",
      "error": "送信に失敗しました"
    },
    "featureRequest": {
      "title": "機能をリクエスト",
      "titleLabel": "タイトル",
      "titlePlaceholder": "欲しい機能を簡潔に説明",
      "descriptionLabel": "説明",
      "descriptionPlaceholder": "どんな機能が欲しいですか？\n- どんな問題を解決しますか？\n- どのように使いたいですか？",
      "anonymous": "匿名で送信",
      "anonymousHint": "ユーザー名・メールを含めない",
      "submit": "送信",
      "success": "機能リクエストを送信しました",
      "error": "送信に失敗しました"
    },
    "validation": {
      "titleRequired": "タイトルは必須です",
      "titleMinLength": "タイトルは5文字以上で入力してください",
      "titleMaxLength": "タイトルは100文字以内で入力してください",
      "descriptionRequired": "説明は必須です",
      "descriptionMinLength": "説明は20文字以上で入力してください",
      "descriptionMaxLength": "説明は2000文字以内で入力してください"
    },
    "rateLimit": "送信制限に達しました。しばらく待ってから再度お試しください"
  }
}
```

---

## 8. 参考リンク

- [GitHub REST API - Issues](https://docs.github.com/en/rest/issues/issues)
- [Personal Access Token作成方法](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token)
