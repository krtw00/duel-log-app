# ========================================
# Application Configuration
# ========================================
ENVIRONMENT=development

# ========================================
# Database Configuration
# ========================================
POSTGRES_USER=duellog_user
POSTGRES_PASSWORD=duellog_password
POSTGRES_DB=duellog_db
DATABASE_URL=postgresql://duellog_user:duellog_password@db:5432/duellog_db

# ========================================
# JWT Configuration
# ========================================
# SECRET_KEYは必ず本番環境では変更してください
# 以下のコマンドで安全なランダム文字列を生成できます（base64形式を使用してください）:
#
# 方法1（推奨）: OpenSSLでbase64形式で生成
#   openssl rand -base64 32
#
# 方法2: Pythonで生成
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# 注意: openssl rand -hex 32 はエラーの原因となるため使用しないでください
#
# 生成した文字列を以下のSECRET_KEYに設定してください
SECRET_KEY=your-secret-key-here-replace-with-random-64-character-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ========================================
# Backend Configuration
# ========================================
FRONTEND_URL=http://localhost:5173

# ========================================
# Mail Configuration
# ========================================
MAIL_FROM=your-email@example.com
RESEND_API_KEY=your-resend-api-key

# ========================================
# Frontend Configuration
# ========================================
VITE_API_BASE_URL=http://localhost:8000
